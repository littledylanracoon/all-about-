Q:

每個計算只有兩個正整數或0，而且運算符號只有加(+)、減(-)、乘(*)這三種，現在想請你幫她寫一個程式，輸入這個算式之後，把答案印出來。

輸入資料格式為 A?B，其中 A、B 為正整數或 0， ? 為 +、-、* 這三種符號中的一種。



A:



#include<iostream>

using namespace std;


int A,B,x;
char r;
int main(){

cin>>A>>r>>B;
if(A>=0 && B>=0)
{
 if(r=='+')    //是 '  '   不是  "  " !!!!
    {x=A+B;
     cout<<x<<endl;
}
  if(r=='-')
    {x=A-B;
     cout<<x<<endl;
}
  if(r=='*')
    {x=A*B;
     cout<<x<<endl;


}


}

}






