判斷: 'A' 'a'
轉換: + - 32

#include<iostream>
using namespace std;

int main(){
    
    string a = "hello";
    for(int i = 0; i < a.size(); i++){
        if(a[i] <= 'z' && a[i] >= 'a')
            a[i] -= 32;
    }
    cout << a;
    
}
